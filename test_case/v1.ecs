# ECS Lang (Extended Covariant Script)
import codec.json, network.tcp as tcp_socket

var a = new tcp_socket.socket{"192.168.233.233", 3306}

if a > 0
    system.out.println("a")
else
    if a == 0 &&
         b != 0 &&!
           c >= 0
    system.out.println("b")
else
    system.out.println("c"); {syntax.cond_or(
        {syntax.term("("), syntax.ref("expr"), syntax.term(")")},
        {syntax.ref("object"), syntax.optional(syntax.ref("factor_s"))},
        {syntax.term("{"), syntax.optional(syntax.ref("args")), syntax.term("}")},
        {syntax.token("str")},
        {syntax.token("num")}
    )}
    a.b(c[0], d.e())["Hello"].world()
    *(a->hello)("World!").text
end

test([b, =c](ttx : number, =xxx){
    if ttx > xxx
        system.exit(0)
    end
})

b = [](...xxx)->xxx > 1 ? self(xxx - 2) + self(xxx - 1) : 1

namespace xxx
    using json.xxxx, network.xxxx
    namespace xxx
        var a = 10
    end
end

block
    var (b, (c, d), e) = {1, {2, 3}, 4}
    (b, (c, d), e) = {1, {2, 3}, 4}
end